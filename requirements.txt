# Python Dependencies for docuMINE
# =================================
# These are used by the AI quoting automation features.
# Node.js dependencies are in package.json (main application)
#
# NOTE: Browser Use and LangChain dependencies removed in Q8-0 cleanup.
# Stagehand (Q8-1) uses TypeScript/Node.js - no Python deps needed for automation.

# Playwright - Browser automation (used by Stagehand)
playwright>=1.40.0

# Environment management
python-dotenv>=1.0.0
