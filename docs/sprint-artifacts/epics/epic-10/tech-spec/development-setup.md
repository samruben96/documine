# Development Setup

```bash
# Already cloned and set up (existing project)
cd documine

# Ensure dependencies installed
npm install

# Start local dev server
npm run dev

# Run tests
npm test

# Run E2E tests
npx playwright test

# Type check
npx tsc --noEmit
```

---
